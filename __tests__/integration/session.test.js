//possui vários its
describe('Authentication', () => {
    //descritivo do teste
    it('should sum two numbers', () => {
        const x = 2;
        const y = 4;

        const sum = x + y;
        //resultado esperado
        expect(sum).toBe(6);
    });
});
